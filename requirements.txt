# ─── Web framework ───
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ─── HTTP client ───
httpx>=0.27.0

# ─── Environment ───
python-dotenv>=1.0.0

# ─── AI / LLM ───
openai>=1.59.7

# ─── Speech recognition ───
faster-whisper>=1.1.0

# ─── Speech synthesis (local, offline) ───
piper-tts>=1.4.1
g2pw>=0.1.1            # Chinese phonemizer — indirect dep of piper-tts >=1.4.2
unicode_rbnf>=2.0.0    # Chinese number-to-words — indirect dep of piper-tts >=1.4.2
# PyTorch (CPU-only, ~125 MB) is also required for Chinese TTS.
# Install separately (special index URL):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# ─── Browser automation (Calendar — Playwright mode) ───
playwright>=1.50.1

# ─── Vector search ───
faiss-cpu
numpy>=1.26.0

# ─── NLP / Text ───
dateparser>=1.2.0
opencc-python-reimplemented>=0.1.7

# ─── Schema validation ───
jsonschema>=4.23.0

# ─── MCP (Model Context Protocol) ───
mcp>=1.26.0

# ─── Google Calendar API (API mode, optional) ───
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# ─── Testing ───
pytest>=8.0.0
pytest-asyncio>=0.23.0
